# API Reference

接口文档已经拆成两份：

- `facade-api.md`
  - 面向日常使用
  - 主要包含 `AIMemory` 和 `AsyncAIMemory` 的公开接口
- `service-worker-api.md`
  - 面向进阶开发
  - 主要包含内部 service、worker 和更细粒度的调用方式

建议阅读顺序：

1. 先看 `QUICKSTART.md`
2. 再看 `facade-api.md`
3. 最后按需要查看 `service-worker-api.md`
